const Persons = ({ name, number, handle, persons }) => {
  return (
    <div>
      <form onSubmit={handle}>
        name <input {...name} /> <br />
        number <input {...number} />
        <button>create</button>
      </form>
      {persons.map((n) => (
        <p key={n.id}>
          {n.name} {n.number}
        </p>
      ))}
    </div>
  );
};

export default Persons